# 📦 代码推送到 GitHub 完全指南

> **傻瓜式操作文档** - 从零开始，手把手教你将代码推送到 GitHub 并实现云端同步

---

## 📚 目录

1. [准备工作](#准备工作)
2. [创建 GitHub 仓库](#创建-github-仓库)
3. [配置 Git](#配置-git)
4. [推送代码到 GitHub](#推送代码到-github)
5. [实时同步](#实时同步)
6. [常用命令速查](#常用命令速查)
7. [团队协作](#团队协作)
8. [常见问题](#常见问题)

---

## ⏱️ 预计时间

- 首次设置：**15-20 分钟**
- 日常使用：**1-2 分钟/次**

---

## 🎯 准备工作

### 步骤 1.1：注册 GitHub 账号

1. 访问 https://github.com
2. 点击右上角 **Sign up**（注册）
3. 填写信息：
   - 邮箱地址
   - 密码（至少8位）
   - 用户名（唯一，建议使用英文）
4. 完成验证（拼图或问题）
5. 选择免费计划（Free）
6. 验证邮箱

### 步骤 1.2：检查 Git 是否已安装

打开终端，输入：

```bash
git --version
```

**预期输出：**
```
git version 2.x.x
```

**如果显示 "command not found"：**

- **macOS**：会自动提示安装，点击"安装"即可
- **Windows**：下载安装 https://git-scm.com/download/win
- **Linux**：运行 `sudo apt-get install git`

---

## 🚀 创建 GitHub 仓库

### 步骤 2.1：创建新仓库

1. 登录 GitHub
2. 点击右上角 **+** → **New repository**
3. 填写仓库信息：

```
Repository name: content-factory
Description: 智能内容创作与发布管理平台
Public / Private: 选择 Private（私有，推荐）
✅ 不要勾选 "Add a README file"
✅ 不要选择 .gitignore
✅ 不要选择 License
```

4. 点击 **Create repository**

### 步骤 2.2：记录仓库地址

创建完成后，会看到仓库地址，格式类似：

```
https://github.com/你的用户名/content-factory.git
```

**保存这个地址，后面会用到！**

---

## 🔧 配置 Git

### 步骤 3.1：配置用户信息

在终端中执行（替换为你的信息）：

```bash
# 配置用户名
git config --global user.name "你的名字"

# 配置邮箱（与 GitHub 注册邮箱一致）
git config --global user.email "your.email@example.com"

# 验证配置
git config --list
```

### 步骤 3.2：配置 Git 凭证管理

```bash
# macOS - 使用钥匙串保存密码
git config --global credential.helper osxkeychain

# Windows - 使用凭证管理器
git config --global credential.helper wincred

# Linux - 缓存密码 1 小时
git config --global credential.helper 'cache --timeout=3600'
```

### 步骤 3.3：生成 SSH 密钥（推荐，更安全）

**为什么用 SSH？**
- ✅ 不用每次输入密码
- ✅ 更安全
- ✅ GitHub 推荐方式

**操作步骤：**

```bash
# 1. 生成 SSH 密钥（替换为你的邮箱）
ssh-keygen -t ed25519 -C "your.email@example.com"

# 提示：Enter file in which to save the key
# 直接按回车（使用默认路径）

# 提示：Enter passphrase
# 可以留空（直接回车）或设置密码短语

# 2. 启动 ssh-agent
eval "$(ssh-agent -s)"

# 3. 添加私钥到 ssh-agent
ssh-add ~/.ssh/id_ed25519

# 4. 复制公钥到剪贴板
# macOS:
pbcopy < ~/.ssh/id_ed25519.pub

# Linux:
cat ~/.ssh/id_ed25519.pub
# 然后手动复制输出内容

# Windows (Git Bash):
clip < ~/.ssh/id_ed25519.pub
```

**添加 SSH 公钥到 GitHub：**

1. 访问 https://github.com/settings/keys
2. 点击 **New SSH key**
3. Title：填写 `MacBook Pro`（或你的设备名）
4. Key：粘贴刚才复制的公钥
5. 点击 **Add SSH key**

**测试连接：**

```bash
ssh -T git@github.com
```

**预期输出：**
```
Hi 你的用户名! You've successfully authenticated...
```

---

## 📤 推送代码到 GitHub

### 步骤 4.1：初始化本地仓库

在你的项目目录中执行：

```bash
cd /Users/jiangchenxiang1/Desktop/content-factory

# 检查是否已初始化
git status
```

**如果显示 "not a git repository"：**

```bash
# 初始化 Git 仓库
git init
```

### 步骤 4.2：创建 .gitignore 文件

这个文件告诉 Git 哪些文件不需要提交到 GitHub。

**我已经为你准备好了，直接复制使用！**

### 步骤 4.3：添加所有文件

```bash
# 查看将要提交的文件
git status

# 添加所有文件到暂存区
git add .

# 再次查看状态
git status
```

### 步骤 4.4：创建首次提交

```bash
git commit -m "Initial commit: 内容工厂项目初始化"
```

### 步骤 4.5：连接远程仓库

**如果使用 HTTPS（简单，但需要输入密码）：**

```bash
git remote add origin https://github.com/你的用户名/content-factory.git
```

**如果使用 SSH（推荐，已配置密钥）：**

```bash
git remote add origin git@github.com:你的用户名/content-factory.git
```

**验证远程仓库：**

```bash
git remote -v
```

**预期输出：**
```
origin  git@github.com:你的用户名/content-factory.git (fetch)
origin  git@github.com:你的用户名/content-factory.git (push)
```

### 步骤 4.6：推送代码到 GitHub

```bash
# 设置默认分支为 main
git branch -M main

# 推送代码到 GitHub
git push -u origin main
```

**首次推送可能需要输入 GitHub 密码或确认 SSH 连接。**

**推送成功后，访问你的 GitHub 仓库，应该能看到所有代码了！** 🎉

---

## 🔄 实时同步

### 日常工作流程

每次修改代码后，执行以下 3 步：

```bash
# 1️⃣ 查看修改了哪些文件
git status

# 2️⃣ 添加修改到暂存区
git add .
# 或只添加特定文件：
# git add 文件路径

# 3️⃣ 提交并推送
git commit -m "描述你做了什么修改"
git push
```

### 快捷命令（推荐）

我为你创建了一个快捷脚本，3 秒完成提交推送！

**使用方法：**

```bash
# 提交并推送，自动生成提交信息
npm run push

# 或带自定义提交信息
npm run push "修复了登录bug"
```

### 自动同步脚本

如果你想每隔一段时间自动推送到 GitHub，可以使用定时任务。

---

## 📖 常用命令速查

### 基础操作

```bash
# 查看状态
git status

# 查看修改内容
git diff

# 查看提交历史
git log
git log --oneline  # 简洁版

# 查看远程仓库
git remote -v
```

### 提交操作

```bash
# 添加文件
git add 文件名              # 添加单个文件
git add .                  # 添加所有修改
git add src/               # 添加整个目录

# 提交
git commit -m "提交信息"
git commit -am "提交信息"  # 添加并提交（仅跟踪的文件）

# 修改最后一次提交
git commit --amend -m "新的提交信息"
```

### 推送和拉取

```bash
# 推送到远程仓库
git push                    # 推送到默认分支
git push origin main        # 推送到 main 分支

# 拉取远程更新
git pull                    # 拉取并合并
git fetch                   # 只拉取不合并
```

### 分支操作

```bash
# 查看分支
git branch                  # 查看本地分支
git branch -a               # 查看所有分支

# 创建分支
git branch 分支名
git checkout -b 分支名      # 创建并切换

# 切换分支
git checkout 分支名
git switch 分支名           # Git 2.23+ 推荐

# 合并分支
git merge 分支名

# 删除分支
git branch -d 分支名        # 删除已合并的分支
git branch -D 分支名        # 强制删除
```

### 撤销操作

```bash
# 撤销工作区修改
git checkout -- 文件名
git restore 文件名          # Git 2.23+ 推荐

# 撤销暂存区
git reset HEAD 文件名
git restore --staged 文件名  # Git 2.23+ 推荐

# 回退到上一个提交
git reset --soft HEAD~1     # 保留修改
git reset --hard HEAD~1     # 丢弃修改（危险）

# 撤销远程提交
git revert 提交ID
```

---

## 👥 团队协作

### 多人开发流程

```bash
# 1. 克隆项目（新成员加入）
git clone git@github.com:用户名/content-factory.git
cd content-factory

# 2. 创建功能分支
git checkout -b feature/新功能名称

# 3. 开发并提交
git add .
git commit -m "实现了XX功能"

# 4. 推送分支到远程
git push -u origin feature/新功能名称

# 5. 在 GitHub 上创建 Pull Request
# 访问仓库页面，点击 "Compare & pull request"

# 6. 代码审查通过后，合并到主分支
# 在 GitHub 上点击 "Merge pull request"

# 7. 更新本地主分支
git checkout main
git pull
```

### 解决冲突

当多人修改同一文件时，可能会产生冲突：

```bash
# 1. 拉取最新代码
git pull

# 如果有冲突，Git 会提示：
# CONFLICT (content): Merge conflict in 文件名

# 2. 打开冲突文件，会看到：
<<<<<<< HEAD
你的修改
=======
别人的修改
>>>>>>> 远程分支

# 3. 手动编辑，保留需要的内容，删除标记

# 4. 标记为已解决
git add 文件名

# 5. 完成合并
git commit -m "解决了XX冲突"
git push
```

---

## 🔐 安全和最佳实践

### .gitignore 配置

**绝对不要提交的文件：**

```gitignore
# 敏感信息
.env
.env.local
.env.production
*.key
*.pem

# 数据库文件
*.db
*.sqlite
*.sqlite3

# 依赖目录
node_modules/
.pnp/

# 构建产物
.next/
out/
dist/
build/

# 日志文件
*.log
npm-debug.log*

# 系统文件
.DS_Store
Thumbs.db
```

### 敏感信息保护

**如果不小心提交了敏感信息：**

```bash
# 1. 从 Git 历史中彻底删除
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch .env.local" \
  --prune-empty --tag-name-filter cat -- --all

# 2. 强制推送到远程
git push origin --force --all

# 3. 立即更改泄露的密钥/密码
```

### 提交信息规范

使用规范的提交信息，方便追踪和回滚：

```bash
# 格式：<类型>: <描述>

# 类型：
feat: 新功能
fix: 修复 bug
docs: 文档更新
style: 代码格式（不影响功能）
refactor: 重构代码
test: 测试相关
chore: 构建/工具配置

# 示例：
git commit -m "feat: 添加用户登录功能"
git commit -m "fix: 修复文章保存失败的问题"
git commit -m "docs: 更新 README 安装说明"
```

---

## ❓ 常见问题

### Q1: 推送时提示 "Permission denied"

**原因：** SSH 密钥未配置或配置错误

**解决：**
```bash
# 测试 SSH 连接
ssh -T git@github.com

# 如果失败，重新配置 SSH 密钥（参见步骤 3.3）
```

### Q2: 推送时提示 "rejected"

**原因：** 远程仓库有更新，本地代码不是最新的

**解决：**
```bash
# 先拉取远程更新
git pull --rebase

# 解决可能的冲突后
git push
```

### Q3: 忘记提交信息怎么办？

```bash
# 修改最后一次提交信息
git commit --amend -m "新的提交信息"

# 如果已经推送，需要强制推送
git push --force
```

### Q4: 想撤销刚才的推送

```bash
# 回退一个提交（本地）
git reset --hard HEAD~1

# 强制推送到远程
git push --force
```

**⚠️ 警告：`--force` 会覆盖远程历史，团队项目慎用！**

### Q5: 如何查看某个文件的修改历史？

```bash
# 查看文件的所有提交
git log --follow 文件路径

# 查看每次提交的具体改动
git log -p 文件路径
```

### Q6: 项目太大，推送很慢

```bash
# 使用浅克隆（只保留最近的提交）
git clone --depth 1 git@github.com:用户名/仓库.git

# 已有项目，压缩历史
git gc --aggressive --prune=now
```

---

## 🎓 进阶技巧

### 使用 Git Hooks 自动化

在 `.git/hooks/pre-commit` 中添加：

```bash
#!/bin/sh
# 提交前自动运行测试
npm test
```

### Git Alias（命令别名）

```bash
# 配置常用别名
git config --global alias.st status
git config --global alias.co checkout
git config --global alias.ci commit
git config --global alias.br branch
git config --global alias.lg "log --oneline --graph --all"

# 使用
git st      # 相当于 git status
git lg      # 漂亮的提交历史图
```

### Cherry-pick（挑选提交）

```bash
# 将特定提交应用到当前分支
git cherry-pick 提交ID
```

### Stash（暂存工作）

```bash
# 暂存当前修改
git stash

# 查看暂存列表
git stash list

# 恢复暂存
git stash pop

# 清空所有暂存
git stash clear
```

---

## 📱 推荐工具

### 图形界面工具

- **GitHub Desktop**：官方客户端，适合新手
  - 下载：https://desktop.github.com/

- **SourceTree**：功能强大，免费
  - 下载：https://www.sourcetreeapp.com/

- **GitKraken**：界面漂亮，跨平台
  - 下载：https://www.gitkraken.com/

### VS Code 扩展

- **GitLens**：查看代码历史和作者
- **Git Graph**：可视化提交历史
- **GitHub Pull Requests**：在 VS Code 中处理 PR

---

## 📞 获取帮助

- **Git 官方文档**：https://git-scm.com/doc
- **GitHub 文档**：https://docs.github.com/
- **交互式教程**：https://learngitbranching.js.org/
- **命令速查表**：https://training.github.com/downloads/zh_CN/github-git-cheat-sheet/

---

## ✅ 完成检查清单

- [ ] 创建了 GitHub 账号
- [ ] 安装并配置了 Git
- [ ] 创建了 GitHub 仓库
- [ ] 配置了 SSH 密钥
- [ ] 成功推送了代码
- [ ] 理解了基本的 Git 工作流程
- [ ] 配置了 .gitignore
- [ ] 了解了常用命令
- [ ] 知道如何解决常见问题

---

**🎉 恭喜！你已经掌握了 Git 和 GitHub 的基本使用！**

**下次修改代码后，只需执行：**
```bash
git add .
git commit -m "描述你的修改"
git push
```

**或使用快捷脚本：**
```bash
npm run push "你的提交信息"
```

---

**文档版本：** 1.0.0
**更新时间：** 2025-11-08
**适用系统：** macOS / Windows / Linux

# 🚀 快速部署到 Vercel - 5分钟搞定！

## 方法一：使用一键部署脚本（推荐）

在项目根目录运行：

```bash
./deploy.sh
```

按照提示输入信息即可。

## 方法二：手动部署

### 步骤 1：配置 Git（首次使用）

```bash
git config --global user.name "你的名字"
git config --global user.email "your.email@example.com"
```

### 步骤 2：提交代码

```bash
git add .
git commit -m "初始提交"
```

### 步骤 3：创建 GitHub 仓库

1. 访问：https://github.com/new
2. 仓库名：`content-factory`
3. 选择：Public 或 Private
4. **不要**勾选 README 和 .gitignore
5. 点击 **Create repository**

### 步骤 4：推送到 GitHub

```bash
# 替换 YOUR_USERNAME 为你的 GitHub 用户名
git remote add origin https://github.com/YOUR_USERNAME/content-factory.git
git branch -M main
git push -u origin main
```

### 步骤 5：部署到 Vercel

1. 访问：https://vercel.com
2. 点击：**Continue with GitHub**
3. 点击：**Add New...** → **Project**
4. 找到：**content-factory** 仓库
5. 点击：**Import**
6. 点击：**Deploy**

等待 2-3 分钟，部署完成！

## 访问你的应用

部署成功后，Vercel 会给你一个地址：
```
https://content-factory-xxx.vercel.app
```

点击访问即可！

## 常见问题

### Q: 推送到 GitHub 时要求输入密码怎么办？

A: GitHub 已不支持密码登录，需要使用 Personal Access Token：

1. 访问：https://github.com/settings/tokens
2. 点击：**Generate new token (classic)**
3. 勾选：**repo** 权限
4. 生成后，复制 token
5. 推送时，用户名输入你的 GitHub 用户名，密码输入这个 token

### Q: 数据库怎么办？

A: SQLite 不适合 Vercel，建议：

**选项 1：Vercel Postgres（推荐）**
- 在 Vercel 项目设置中添加 Postgres 数据库
- 免费额度足够使用

**选项 2：Supabase（免费）**
- 访问：https://supabase.com
- 创建项目，获取数据库连接信息
- 在 Vercel 环境变量中配置

### Q: 如何更新代码？

A: 修改代码后：

```bash
git add .
git commit -m "更新说明"
git push
```

Vercel 会自动检测并重新部署！

## 下一步

✅ 部署成功后，建议：

1. **添加自定义域名**（可选）
   - Vercel → Settings → Domains

2. **配置环境变量**
   - Vercel → Settings → Environment Variables
   - 添加 API 密钥等敏感信息

3. **查看部署日志**
   - Vercel → Deployments
   - 查看每次部署的详细日志

4. **启用分析**
   - Vercel → Analytics
   - 监控访问量和性能

## 需要帮助？

查看详细文档：`DEPLOYMENT_GUIDE.md`

---

🎉 祝你部署顺利！
